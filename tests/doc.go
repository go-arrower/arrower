/*
Package tests contains helpers, that enable you to perform integration tests against a real
external service e.g. a postgres database, either within CI or locally against a docker environment.
*/
package tests
